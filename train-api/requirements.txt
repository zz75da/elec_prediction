python-dotenv==1.0.1
pyyaml==6.0.2
requests==2.32.3
pydantic==2.10.4
fastapi==0.115.6
uvicorn[standard]==0.34.0

numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
statsmodels==0.14.4
lightgbm==4.7.0
mlforecast==1.1.0

# mlflow-skinny, not mlflow: the full `mlflow` package hard-depends on pyarrow, whose
# bundled native Arrow runtime causes a real access-violation crash inside LightGBM's
# Dataset construction on Windows the moment both are imported in the same process
# (confirmed by isolating package-by-package — pyarrow was the sole trigger, not numpy/
# scipy/statsmodels versions). mlflow-skinny provides every tracking-client function this
# module actually calls (set_experiment/start_run/log_params/log_metrics/set_tag) without
# pyarrow, matplotlib, Flask, docker, alembic, or graphene — none of which train-api needs
# since it only logs to a remote MLflow server (DagsHub), never runs its own MLflow UI/server.
mlflow-skinny==2.17.2
boto3==1.35.76
dvc[s3]==3.60.0
dagshub==0.7.0

prometheus-client==0.21.0
prometheus-fastapi-instrumentator==7.0.0

pytest==8.3.4
