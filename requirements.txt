# Root requirements — local dev / notebook / running scripts outside Docker.
# Service-specific pinned requirements live in train-api/requirements.txt,
# predict-api/requirements.txt and streamlit/requirements.txt (same split as rakuten_mlops_services).

numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
statsmodels==0.14.4
scikit-learn==1.6.0
openpyxl==3.1.5
lightgbm==4.7.0
mlforecast==1.1.0

matplotlib==3.9.3
seaborn==0.13.2

# mlflow-skinny, not mlflow — see train-api/requirements.txt's comment: the full mlflow
# package hard-depends on pyarrow, which crashes LightGBM natively on Windows the moment
# both are imported in the same process. mlflow-skinny has the same tracking-client API
# (log_params/log_metrics/start_run/set_tag) without pulling in pyarrow.
mlflow-skinny==2.17.2
dagshub==0.7.0
dvc[s3]==3.60.0

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
requests==2.32.3

prometheus-client==0.21.0
prometheus-fastapi-instrumentator==7.0.0

pytest==8.3.4
pytest-cov==6.0.0

python-dotenv==1.0.1
